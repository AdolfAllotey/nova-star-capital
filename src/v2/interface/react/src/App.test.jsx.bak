import React from "react";

export default function App() {
  return (
    <div style={{
      minHeight: "100vh",
      backgroundColor: "#020617",  // proche de bg-zinc-950
      color: "#e5e7eb",            // proche de text-zinc-200
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      flexDirection: "column",
      fontFamily: "system-ui, -apple-system, BlinkMacSystemFont, sans-serif"
    }}>
      <h1 style={{ fontSize: "24px", marginBottom: "8px" }}>
        Nova Star Capital — Test App.jsx
      </h1>
      <p style={{ fontSize: "14px", opacity: 0.8 }}>
        Si tu vois cet écran, React + Vite fonctionnent correctement.
      </p>
    </div>
  );
}
